<li class="folder">
  <a href="#" class="submenu"><%= l(:label_tags) %></a>
  <ul>
    <li><%= context_menu_link l(:button_add),
              catalog_add_tag_path(:issue_ids => @issue_ids, :project_id => @project, :back_url => @back),
              :remote => true %></li>
    <li><%= context_menu_link l(:button_delete),
              catalog_delete_tag_path(:issue_ids => @issue_ids, :project_id => @project, :back_url => @back),
              :remote => true %></li>
  </ul>
</li>
